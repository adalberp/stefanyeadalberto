

<div class="rgtWrap">
  <%= form_for [ post, comment ], :remote => false do |f| %>
    <%= error_messages_for comment %>

    <div class="field">
      <%= f.label :Nome %><br />
      <%= f.text_field :author %>
    </div>

    <div class="field">
      <%= f.label :Email %><br />
      <%= f.text_field :email %>
    </div>

    <div class="field">
      <%= f.label :Conteúdo %><br />
      <%= f.text_area :body, :rows => 10, :cols => 90 %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>

  <% end %>
</div>